<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sigil{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="min-h-screen" style="background-color: var(--sigil-bg); color: var(--sigil-text);">

{# Top-center toast stack: one fixed container so multiple flashes don't overlap #}
<div class="fixed top-4 inset-x-0 z-50 flex flex-col items-center gap-2 px-4 pointer-events-none">
    {% for type, messages in app.flashes(['success', 'danger', 'warning', 'info']) %}
        {% for message in messages %}
            <div class="w-full max-w-md px-4 py-3 rounded-card text-sm font-medium text-center shadow-lg pointer-events-auto
                       {{ type == 'success' ? 'bg-success/10 text-success border border-success/20'
                       : type == 'danger' ? 'bg-danger/10 text-danger border border-danger/20'
                       : type == 'warning' ? 'bg-warning/10 text-warning border border-warning/20'
                       : 'bg-info/10 text-info border border-info/20' }}"
                 style="backdrop-filter: blur(12px);">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
</div>

{% block body %}{% endblock %}

</body>
</html>
