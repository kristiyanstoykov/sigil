# Python side of the image (bin/*.py drivers): every package pinned, transitive
# ones included, so a build is reproducible and `pip-audit -r requirements.txt`
# (CI) covers all of it - the Composer side has `composer audit`, this is its
# counterpart. Regenerate after a deliberate upgrade with
#   docker compose exec app pip freeze | grep -v '^pip=' | sort
# and re-run the E2E signing tests. Keep pyHanko in lockstep with bin/sign_pdf.py
# (0.37 dropped open_pkcs11_session(token_label=), see the driver).
PyYAML==6.0.3
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
asn1crypto==1.5.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
fonttools==4.65.0
frozenlist==1.8.0
idna==3.20
lxml==6.1.3
multidict==6.9.0
oscrypto==1.3.0
pillow==12.3.0
platformdirs==4.11.11
propcache==0.5.4
pyHanko==0.37.0
pycparser==3.0
pyhanko-certvalidator==0.32.1
pyhanko-cli==0.5.0
python-barcode==0.16.1
python-pkcs11==0.9.5
qrcode==8.2
tzlocal==5.4.4
uharfbuzz==0.55.0
uritools==6.1.3
yarl==1.25.1
