; Sigil document uploads. The app enforces the real limit (10 MB, via
; app.max_document_size_bytes) and returns a clean message; PHP is set a little
; above that so the app-level check is the authoritative one for files just over.
upload_max_filesize = 12M
post_max_size = 13M
memory_limit = 256M
